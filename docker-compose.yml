version: '3.8'

services:
  api:
    build: .
    container_name: carecircle-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    command: python main.py --mode api
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  ui:
    build: .
    container_name: carecircle-ui
    ports:
      - "8501:8501"
    env_file:
      - .env
    depends_on:
      - api
    command: streamlit run src/ui/streamlit_app.py --server.port 8501 --server.address 0.0.0.0
    restart: unless-stopped
